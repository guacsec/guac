---
# Version for this file.
version: 1

# (Optional) List of env variables used during compilation.
env:
  - GO111MODULE=on
  - CGO_ENABLED=0
  - PKG=github.com/guacsec/guac/pkg/version
  - GUAC_BIN=guaccollet

# (Optional) Flags for the compiler.
flags:
  - -trimpath
  - -tags=netgo

# The OS to compile for. `GOOS` env variable will be set to this value.
goos: darwin

# The architecture to compile for. `GOARCH` env variable will be set to this value.
goarch: arm64

# (Optional) Entrypoint to compile.
main: ./cmd/{{ .Env.GUAC_BIN }}/main.go

# (Optional) Working directory. (default: root of the project)
# dir: ./relative/path/to/dir
dir: ./

# Binary output name.
# {{ .Os }} will be replaced by goos field in the config file.
# {{ .Arch }} will be replaced by goarch field in the config file.
binary: {{ .Env.GUAC_BIN }}-{{ .Os }}-{{ .Arch }}

# (Optional) ldflags generated dynamically in the workflow, and set as the `evaluated-envs` input variables in the workflow.
ldflags:
  - "-X {{ .Env.PKG }}.Version={{ .Env.VERSION }}"
  - "-X {{ .Env.PKG }}.Commit={{ .Env.COMMIT }}"
  - "-X {{ .Env.PKG }}.Date={{ .Env.COMMIT_DATE }}"
  - "-X {{ .Env.PKG }}.TreeState={{ .Env.TREE_STATE }}"