// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/guacsec/guac/pkg/assembler/backends/ent/occurrence"
	"github.com/guacsec/guac/pkg/assembler/backends/ent/occurrencesubject"
	"github.com/guacsec/guac/pkg/assembler/backends/ent/packageversion"
	"github.com/guacsec/guac/pkg/assembler/backends/ent/predicate"
	"github.com/guacsec/guac/pkg/assembler/backends/ent/sourcename"
)

// OccurrenceSubjectQuery is the builder for querying OccurrenceSubject entities.
type OccurrenceSubjectQuery struct {
	config
	ctx            *QueryContext
	order          []occurrencesubject.OrderOption
	inters         []Interceptor
	predicates     []predicate.OccurrenceSubject
	withOccurrence *OccurrenceQuery
	withPackage    *PackageVersionQuery
	withSource     *SourceNameQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OccurrenceSubjectQuery builder.
func (osq *OccurrenceSubjectQuery) Where(ps ...predicate.OccurrenceSubject) *OccurrenceSubjectQuery {
	osq.predicates = append(osq.predicates, ps...)
	return osq
}

// Limit the number of records to be returned by this query.
func (osq *OccurrenceSubjectQuery) Limit(limit int) *OccurrenceSubjectQuery {
	osq.ctx.Limit = &limit
	return osq
}

// Offset to start from.
func (osq *OccurrenceSubjectQuery) Offset(offset int) *OccurrenceSubjectQuery {
	osq.ctx.Offset = &offset
	return osq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (osq *OccurrenceSubjectQuery) Unique(unique bool) *OccurrenceSubjectQuery {
	osq.ctx.Unique = &unique
	return osq
}

// Order specifies how the records should be ordered.
func (osq *OccurrenceSubjectQuery) Order(o ...occurrencesubject.OrderOption) *OccurrenceSubjectQuery {
	osq.order = append(osq.order, o...)
	return osq
}

// QueryOccurrence chains the current query on the "occurrence" edge.
func (osq *OccurrenceSubjectQuery) QueryOccurrence() *OccurrenceQuery {
	query := (&OccurrenceClient{config: osq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := osq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := osq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(occurrencesubject.Table, occurrencesubject.FieldID, selector),
			sqlgraph.To(occurrence.Table, occurrence.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, occurrencesubject.OccurrenceTable, occurrencesubject.OccurrenceColumn),
		)
		fromU = sqlgraph.SetNeighbors(osq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPackage chains the current query on the "package" edge.
func (osq *OccurrenceSubjectQuery) QueryPackage() *PackageVersionQuery {
	query := (&PackageVersionClient{config: osq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := osq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := osq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(occurrencesubject.Table, occurrencesubject.FieldID, selector),
			sqlgraph.To(packageversion.Table, packageversion.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, occurrencesubject.PackageTable, occurrencesubject.PackageColumn),
		)
		fromU = sqlgraph.SetNeighbors(osq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySource chains the current query on the "source" edge.
func (osq *OccurrenceSubjectQuery) QuerySource() *SourceNameQuery {
	query := (&SourceNameClient{config: osq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := osq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := osq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(occurrencesubject.Table, occurrencesubject.FieldID, selector),
			sqlgraph.To(sourcename.Table, sourcename.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, occurrencesubject.SourceTable, occurrencesubject.SourceColumn),
		)
		fromU = sqlgraph.SetNeighbors(osq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OccurrenceSubject entity from the query.
// Returns a *NotFoundError when no OccurrenceSubject was found.
func (osq *OccurrenceSubjectQuery) First(ctx context.Context) (*OccurrenceSubject, error) {
	nodes, err := osq.Limit(1).All(setContextOp(ctx, osq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{occurrencesubject.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (osq *OccurrenceSubjectQuery) FirstX(ctx context.Context) *OccurrenceSubject {
	node, err := osq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OccurrenceSubject ID from the query.
// Returns a *NotFoundError when no OccurrenceSubject ID was found.
func (osq *OccurrenceSubjectQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = osq.Limit(1).IDs(setContextOp(ctx, osq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{occurrencesubject.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (osq *OccurrenceSubjectQuery) FirstIDX(ctx context.Context) int {
	id, err := osq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OccurrenceSubject entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OccurrenceSubject entity is found.
// Returns a *NotFoundError when no OccurrenceSubject entities are found.
func (osq *OccurrenceSubjectQuery) Only(ctx context.Context) (*OccurrenceSubject, error) {
	nodes, err := osq.Limit(2).All(setContextOp(ctx, osq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{occurrencesubject.Label}
	default:
		return nil, &NotSingularError{occurrencesubject.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (osq *OccurrenceSubjectQuery) OnlyX(ctx context.Context) *OccurrenceSubject {
	node, err := osq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OccurrenceSubject ID in the query.
// Returns a *NotSingularError when more than one OccurrenceSubject ID is found.
// Returns a *NotFoundError when no entities are found.
func (osq *OccurrenceSubjectQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = osq.Limit(2).IDs(setContextOp(ctx, osq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{occurrencesubject.Label}
	default:
		err = &NotSingularError{occurrencesubject.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (osq *OccurrenceSubjectQuery) OnlyIDX(ctx context.Context) int {
	id, err := osq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OccurrenceSubjects.
func (osq *OccurrenceSubjectQuery) All(ctx context.Context) ([]*OccurrenceSubject, error) {
	ctx = setContextOp(ctx, osq.ctx, "All")
	if err := osq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OccurrenceSubject, *OccurrenceSubjectQuery]()
	return withInterceptors[[]*OccurrenceSubject](ctx, osq, qr, osq.inters)
}

// AllX is like All, but panics if an error occurs.
func (osq *OccurrenceSubjectQuery) AllX(ctx context.Context) []*OccurrenceSubject {
	nodes, err := osq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OccurrenceSubject IDs.
func (osq *OccurrenceSubjectQuery) IDs(ctx context.Context) (ids []int, err error) {
	if osq.ctx.Unique == nil && osq.path != nil {
		osq.Unique(true)
	}
	ctx = setContextOp(ctx, osq.ctx, "IDs")
	if err = osq.Select(occurrencesubject.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (osq *OccurrenceSubjectQuery) IDsX(ctx context.Context) []int {
	ids, err := osq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (osq *OccurrenceSubjectQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, osq.ctx, "Count")
	if err := osq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, osq, querierCount[*OccurrenceSubjectQuery](), osq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (osq *OccurrenceSubjectQuery) CountX(ctx context.Context) int {
	count, err := osq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (osq *OccurrenceSubjectQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, osq.ctx, "Exist")
	switch _, err := osq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (osq *OccurrenceSubjectQuery) ExistX(ctx context.Context) bool {
	exist, err := osq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OccurrenceSubjectQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (osq *OccurrenceSubjectQuery) Clone() *OccurrenceSubjectQuery {
	if osq == nil {
		return nil
	}
	return &OccurrenceSubjectQuery{
		config:         osq.config,
		ctx:            osq.ctx.Clone(),
		order:          append([]occurrencesubject.OrderOption{}, osq.order...),
		inters:         append([]Interceptor{}, osq.inters...),
		predicates:     append([]predicate.OccurrenceSubject{}, osq.predicates...),
		withOccurrence: osq.withOccurrence.Clone(),
		withPackage:    osq.withPackage.Clone(),
		withSource:     osq.withSource.Clone(),
		// clone intermediate query.
		sql:  osq.sql.Clone(),
		path: osq.path,
	}
}

// WithOccurrence tells the query-builder to eager-load the nodes that are connected to
// the "occurrence" edge. The optional arguments are used to configure the query builder of the edge.
func (osq *OccurrenceSubjectQuery) WithOccurrence(opts ...func(*OccurrenceQuery)) *OccurrenceSubjectQuery {
	query := (&OccurrenceClient{config: osq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	osq.withOccurrence = query
	return osq
}

// WithPackage tells the query-builder to eager-load the nodes that are connected to
// the "package" edge. The optional arguments are used to configure the query builder of the edge.
func (osq *OccurrenceSubjectQuery) WithPackage(opts ...func(*PackageVersionQuery)) *OccurrenceSubjectQuery {
	query := (&PackageVersionClient{config: osq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	osq.withPackage = query
	return osq
}

// WithSource tells the query-builder to eager-load the nodes that are connected to
// the "source" edge. The optional arguments are used to configure the query builder of the edge.
func (osq *OccurrenceSubjectQuery) WithSource(opts ...func(*SourceNameQuery)) *OccurrenceSubjectQuery {
	query := (&SourceNameClient{config: osq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	osq.withSource = query
	return osq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SourceID int `json:"source_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OccurrenceSubject.Query().
//		GroupBy(occurrencesubject.FieldSourceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (osq *OccurrenceSubjectQuery) GroupBy(field string, fields ...string) *OccurrenceSubjectGroupBy {
	osq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OccurrenceSubjectGroupBy{build: osq}
	grbuild.flds = &osq.ctx.Fields
	grbuild.label = occurrencesubject.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SourceID int `json:"source_id,omitempty"`
//	}
//
//	client.OccurrenceSubject.Query().
//		Select(occurrencesubject.FieldSourceID).
//		Scan(ctx, &v)
func (osq *OccurrenceSubjectQuery) Select(fields ...string) *OccurrenceSubjectSelect {
	osq.ctx.Fields = append(osq.ctx.Fields, fields...)
	sbuild := &OccurrenceSubjectSelect{OccurrenceSubjectQuery: osq}
	sbuild.label = occurrencesubject.Label
	sbuild.flds, sbuild.scan = &osq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OccurrenceSubjectSelect configured with the given aggregations.
func (osq *OccurrenceSubjectQuery) Aggregate(fns ...AggregateFunc) *OccurrenceSubjectSelect {
	return osq.Select().Aggregate(fns...)
}

func (osq *OccurrenceSubjectQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range osq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, osq); err != nil {
				return err
			}
		}
	}
	for _, f := range osq.ctx.Fields {
		if !occurrencesubject.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if osq.path != nil {
		prev, err := osq.path(ctx)
		if err != nil {
			return err
		}
		osq.sql = prev
	}
	return nil
}

func (osq *OccurrenceSubjectQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OccurrenceSubject, error) {
	var (
		nodes       = []*OccurrenceSubject{}
		_spec       = osq.querySpec()
		loadedTypes = [3]bool{
			osq.withOccurrence != nil,
			osq.withPackage != nil,
			osq.withSource != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OccurrenceSubject).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OccurrenceSubject{config: osq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, osq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := osq.withOccurrence; query != nil {
		if err := osq.loadOccurrence(ctx, query, nodes, nil,
			func(n *OccurrenceSubject, e *Occurrence) { n.Edges.Occurrence = e }); err != nil {
			return nil, err
		}
	}
	if query := osq.withPackage; query != nil {
		if err := osq.loadPackage(ctx, query, nodes, nil,
			func(n *OccurrenceSubject, e *PackageVersion) { n.Edges.Package = e }); err != nil {
			return nil, err
		}
	}
	if query := osq.withSource; query != nil {
		if err := osq.loadSource(ctx, query, nodes, nil,
			func(n *OccurrenceSubject, e *SourceName) { n.Edges.Source = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (osq *OccurrenceSubjectQuery) loadOccurrence(ctx context.Context, query *OccurrenceQuery, nodes []*OccurrenceSubject, init func(*OccurrenceSubject), assign func(*OccurrenceSubject, *Occurrence)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*OccurrenceSubject)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(occurrence.FieldSubjectID)
	}
	query.Where(predicate.Occurrence(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(occurrencesubject.OccurrenceColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SubjectID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "subject_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (osq *OccurrenceSubjectQuery) loadPackage(ctx context.Context, query *PackageVersionQuery, nodes []*OccurrenceSubject, init func(*OccurrenceSubject), assign func(*OccurrenceSubject, *PackageVersion)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*OccurrenceSubject)
	for i := range nodes {
		if nodes[i].PackageID == nil {
			continue
		}
		fk := *nodes[i].PackageID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(packageversion.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "package_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (osq *OccurrenceSubjectQuery) loadSource(ctx context.Context, query *SourceNameQuery, nodes []*OccurrenceSubject, init func(*OccurrenceSubject), assign func(*OccurrenceSubject, *SourceName)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*OccurrenceSubject)
	for i := range nodes {
		if nodes[i].SourceID == nil {
			continue
		}
		fk := *nodes[i].SourceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(sourcename.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "source_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (osq *OccurrenceSubjectQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := osq.querySpec()
	_spec.Node.Columns = osq.ctx.Fields
	if len(osq.ctx.Fields) > 0 {
		_spec.Unique = osq.ctx.Unique != nil && *osq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, osq.driver, _spec)
}

func (osq *OccurrenceSubjectQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(occurrencesubject.Table, occurrencesubject.Columns, sqlgraph.NewFieldSpec(occurrencesubject.FieldID, field.TypeInt))
	_spec.From = osq.sql
	if unique := osq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if osq.path != nil {
		_spec.Unique = true
	}
	if fields := osq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, occurrencesubject.FieldID)
		for i := range fields {
			if fields[i] != occurrencesubject.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if osq.withPackage != nil {
			_spec.Node.AddColumnOnce(occurrencesubject.FieldPackageID)
		}
		if osq.withSource != nil {
			_spec.Node.AddColumnOnce(occurrencesubject.FieldSourceID)
		}
	}
	if ps := osq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := osq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := osq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := osq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (osq *OccurrenceSubjectQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(osq.driver.Dialect())
	t1 := builder.Table(occurrencesubject.Table)
	columns := osq.ctx.Fields
	if len(columns) == 0 {
		columns = occurrencesubject.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if osq.sql != nil {
		selector = osq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if osq.ctx.Unique != nil && *osq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range osq.predicates {
		p(selector)
	}
	for _, p := range osq.order {
		p(selector)
	}
	if offset := osq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := osq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OccurrenceSubjectGroupBy is the group-by builder for OccurrenceSubject entities.
type OccurrenceSubjectGroupBy struct {
	selector
	build *OccurrenceSubjectQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (osgb *OccurrenceSubjectGroupBy) Aggregate(fns ...AggregateFunc) *OccurrenceSubjectGroupBy {
	osgb.fns = append(osgb.fns, fns...)
	return osgb
}

// Scan applies the selector query and scans the result into the given value.
func (osgb *OccurrenceSubjectGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, osgb.build.ctx, "GroupBy")
	if err := osgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OccurrenceSubjectQuery, *OccurrenceSubjectGroupBy](ctx, osgb.build, osgb, osgb.build.inters, v)
}

func (osgb *OccurrenceSubjectGroupBy) sqlScan(ctx context.Context, root *OccurrenceSubjectQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(osgb.fns))
	for _, fn := range osgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*osgb.flds)+len(osgb.fns))
		for _, f := range *osgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*osgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := osgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OccurrenceSubjectSelect is the builder for selecting fields of OccurrenceSubject entities.
type OccurrenceSubjectSelect struct {
	*OccurrenceSubjectQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (oss *OccurrenceSubjectSelect) Aggregate(fns ...AggregateFunc) *OccurrenceSubjectSelect {
	oss.fns = append(oss.fns, fns...)
	return oss
}

// Scan applies the selector query and scans the result into the given value.
func (oss *OccurrenceSubjectSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, oss.ctx, "Select")
	if err := oss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OccurrenceSubjectQuery, *OccurrenceSubjectSelect](ctx, oss.OccurrenceSubjectQuery, oss, oss.inters, v)
}

func (oss *OccurrenceSubjectSelect) sqlScan(ctx context.Context, root *OccurrenceSubjectQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(oss.fns))
	for _, fn := range oss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*oss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := oss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
